<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Board">
    <insert id="save" parameterType="board">
        insert into board_table(boardTitle, boardWriter, boardPassword, boardContents)
        values (#{boardTitle}, #{boardWriter}, #{boardPassword}, #{boardContents})
    </insert>
    <select id="findAll" resultType="board">
        select *
        from board_table
    </select>
    <select id="findById" parameterType="Long" resultType="board">
        select *
        from board_table
        where id = #{id}
    </select>
<!--    <delete id="delete" parameterType="Long">-->
<!--        delete-->
<!--        from board_table-->
<!--        where id = #{id}-->
<!--    </delete>-->
<!--        <select id="passwordCheck" parameterType="String" resultType="member">-->
<!--            select *-->
<!--            from member_table-->
<!--            where boardPassword = #{boardPassword}-->
<!--        </select>-->
    <!--    <update id="update" parameterType="member">-->
    <!--        update member_table-->
    <!--        set memberPhone=#{memberPhone}-->
    <!--        where id = #{id}-->
    <!--    </update>-->
    <!--    <select id="duplicateCheck" parameterType="String" resultType="String">-->
    <!--        select memberId-->
    <!--        from member_table-->
    <!--        where memberId = #{memberId}-->
    <!--    </select>-->
</mapper>














